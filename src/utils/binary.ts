export const convertBinaryToDecimal = (binary: string): string => parseInt(binary, 2).toString()

export const convertBinaryToOctal = (binary: string): string => parseInt(binary, 2).toString(8)

export const convertBinaryToHexadecimal = (binary: string): string => parseInt(binary, 2).toString(16)
