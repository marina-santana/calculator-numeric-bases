export const convertDecimalToBinary = (decimal: string): string => Number(decimal).toString(2)

export const convertDecimalToOctal = (decimal: string): string => Number(decimal).toString(8)

export const convertDecimalToHexadecimal = (decimal: string): string => Number(decimal).toString(16)